argues that previous research on dispositional ? has ? to ? into account how the inference process may be affected by variations in schematic representation of dispositional attributes  EOL the present article ? a schema as a set of ? links between dispositional levels and categories of relevant behaviors  EOL three general schemata are discussed the partially restrictive schema the hierarchically restrictive schema and the ? restrictive schema each having different implications for the rules of inference ? in making ? based on observed behaviors  EOL relevant research on utilization of information ? consensus situational context and ? past behavior is reviewed in this framework  EOL